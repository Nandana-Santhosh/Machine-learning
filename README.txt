-> A cv project on classifying lettuce and spinach.
-> The 'keras_Model.h5' was made using 'Teachable Machine'
-> There is a custom dataset which include training, test and validation data with which we can train 
   our own model using CNN -- 'train.py'
-> 'classification.py' is the cv code


in a python environment - install the required libraries

   pip install -r requirements.txt

Run the program - 'classification.py"

To quit press 'q'